# 六维评分与简化优化：本地验收

已恢复六维分数和雷达图，并接入模块选择、正文标注、补充问答、逐项采用及手动复评。新流程使用 `resume_score_v2` / `single_pass_v1` 和 `json_structure_v1`，不调用量表或事实审核，不使用内容正则规则拒绝改写。权限、版本冲突、保存屏障、目标字段范围、排序身份和 HTML 清理继续保留。

## 自动化证据

- 后端关联回归共 751 项：746 项通过，5 项按隔离数据库等既有条件跳过。日志：`backend/score-backend-tests.log`。
- 新链路的 12 项测试通过，包含真实服务函数的评分上下文、应用、结果读取和撤销；AI 和数据库操作使用测试替身。日志：`backend/score-new-tests.log`。
- 前端关联组共 230 项：初次运行 228 项通过，2 项旧结构断言在更新后，其所在 26 项测试组全部通过；最后的入口、工作区、流程和 AI 布局 43 项关联复验通过。日志：`score-frontend-tests.log`、`score-recheck.log`、`score-final-ui-recheck.log`。
- 新增挂载式流程测试证明：应用完成时评分调用、复评 claim 和 finalize 均为 0；用户明确点击重新评分后仅增加一次评分调用，应用次数不增加。日志：`score-new-flow.log`。
- TypeScript 检查与生产构建通过；日志：`score-typecheck.log`、`score-build.log`。

## 浏览器与导出

- 内嵌浏览器手动检查桌面及 390×844 布局：雷达图、模块勾选、正文定位和展开评语、跳过补充、内联对比、逐项采用及应用状态可用，控制台没有错误。
- `node tools/qa-resume-score.mjs` 用独立 Chromium 检查同一合成夹具，并生成本目录的桌面、移动端截图和 PDF。默认访问本地 5173，可用 `RESUME_SCORE_QA_ORIGIN` 指定其他本地端口。
- `checks.json` 记录模拟计数：勾选前 0 次调用、规划 1 次、跳过补充 0 次、应用额外 AI 调用 0 次；导出文档没有标注节点。
- `print.pdf` 成功生成。逐页提取文本确认有简历正文、没有“可优化”“改进方向”“六维简历评分”或模拟计数文案；`print-page.png` 是第一页渲染检查。

开发预览：`/__dev/resume-template-preview?templateId=modern-slate&scoreReview=1`。追加 `scorePrint=1` 使用与打印共用的只读文档组件。

## 证据边界

浏览器夹具使用虚构材料和模拟优化结果，不登录用户账户、不写业务数据库、不调用真实模型。后端测试检查生产服务的调用、结构和保存契约；浏览器夹具检查组件及展示。这些结果不代表真实模型内容质量、登录账户端到端流程、隔离 PostgreSQL、托管导出服务或生产部署已经验收。

原有工具栏未提交修改已保留；本次未提交、推送或部署。

## 2026-09-08 实际侧边栏跟进

实际账户仍显示旧指导报告的原因有两处：已保存的 `guidance_audit_v1` 被继续作为主报告呈现；本地 8000 端口的常驻 Uvicorn 进程还加载着旧代码。此前合成夹具的验证没有覆盖常驻后端版本。

- 侧边栏现在始终以六维评分为主。没有数值的历史指导显示“待评分”雷达坐标和显式生成入口，历史正文默认折叠，不将等级转换为假分数，也不自动调用模型。
- 已按原来的 localhost:8000 命令和 backend 环境重启本地后端。实际 `/ready` 返回 ready；OpenAPI 已出现 `selected_suggestion_ids`，当前策略默认值为 `json_structure_v1`。
- 在用户当前打开的实际账户侧边栏确认热更新后的界面；未点击真实 AI 生成按钮，未更改简历内容。
- 189 项关联前端回归通过，类型检查和生产构建通过。日志：`score-sidebar-regressions.log`、`score-sidebar-typecheck.log`、`score-sidebar-build.log`。

## 2026-09-08 侧栏关闭与恢复

- 同一简历内，侧栏关闭后保留上次的报告或助手页面；重新点击 AI / 三栏布局恢复该页面。桌面报告保持挂载，保留 JD / 简历诊断页签；关闭时隐藏并禁止焦点进入。切换账号或简历时清除记忆。
- 优化工作区恢复使用已有方案，延迟恢复不能覆盖用户之后的收起操作。
- 实际账户桌面验证：诊断报告关闭后通过 AI 布局恢复原页签；JD 报告切到列表布局后通过三栏布局恢复原页签。没有调用模型或修改简历正文。
- 关联回归 210 项通过；补充异步恢复保护后受影响的 23 项复验通过。最终 TypeScript 检查、生产构建和差异空白检查通过。日志：`sidebar-restore-regressions.log`、`sidebar-restore-final-tests.log`、`sidebar-restore-typecheck.log`、`sidebar-restore-build.log`。

## 2026-09-08 实际生成失败修复

- 用户两次失败的用量记录均显示提供方响应成功；本地单次复现记录 `invalid_json`。检查确认新链路直接解析带完整 Markdown 外壳的 JSON 时，会拒绝合法块内对象；原失败响应正文未保留，不据此断言每次失败的完整输出形态。评分及按需优化现在共用独立外壳解码器；错误 JSON、解释文字、额外尾随对象仍失败，不使用旧修复解析器，也不增加模型调用。
- 错误提示使用六维评分文案，结构错误明确提示 JSON / 必要字段无效；不展示提供方原始正文。新 `resume_score_v2` 报告不会再被仅识别旧指导版本的前端逻辑立即标记过期；签名变化、缺失附件和已过期状态仍生效。
- 本次实际账户验收调用两次模型：修复前单次失败复现，修复后单次生成成功。界面显示总分 81 与全部六维；数据库保存 `resume_score_v2` / `single_pass_v1`、六维齐全且未过期。新打开的页面恢复同一评分及优化选择入口。没有启动优化、内容改写或批量模型验收。
- 101 项前端评分 / 保存 / AI 布局回归及 179 项优化前端回归通过，172 项后端评分 / 版本 / 历史语义回归通过；优化后端 582 项中 577 项通过、5 项跳过。类型检查、生产构建通过。日志使用 `score-error-*` 前缀。

## 2026-09-08 所选模块优化入口修复

- 实际账户启动失败发生在创建方案 / 调用模型之前。只读调用生产上下文函数定位到两处快照差异：空经历日期在签名中为 `null`、服务端省略字段；旧 `experience_atoms` 富文本副本与当前投影不同，但新评分并不使用此副本。
- 经历行的空可选日期现在兼容缺省与 `null`；新启动仅比较评分使用的简历和目标岗位。具体日期 / 简历内容变化、签名、版本令牌、账号隔离与所选经历链接校验仍保留。新增生产入口回归覆盖空日期及旧副本兼容，也证明实际日期变化仍被拒绝，预检查没有模型调用。
- 重启本地后端后，实际账户一次规划成功：3 个所选建议、3 个改写条目、1 个补充问题，状态 `awaiting_answers`，所有条目的建议 ID 均来自选择。保留在方案页供用户继续，未提交答案或应用改写。
- 后端 756 项回归中 751 项通过、5 项既有跳过；前端评分、保存预检、规范化及 AI 布局 64 项通过。日志：`backend/optimization-entry-regressions.log`、`optimization-entry-frontend.log`。本次无前端源码变更，上一轮类型检查和生产构建证据仍适用。
