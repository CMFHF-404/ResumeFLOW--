# 分批完整盲测：前三批进度

本轮尚未完成，不能宣称达到 80% 目标。已完成 9/30 次基线评分，其中 7 次成功、2 次失败，已执行请求成功率 77.8%。优化、复评分和匿名盲评尚未开始；未执行阶段没有从协议中删除。

| 阶段 | 预定请求 | 已执行 | 成功 | 失败 | 待执行 |
|---|---:|---:|---:|---:|---:|
| 基线评分 | 30 | 9 | 7 | 2 | 21 |
| 优化 | 20 | 0 | 0 | 0 | 20 |
| 复评分 | 30 | 0 | 0 | 0 | 30 |
| 匿名成对盲评 | 10 | 0 | 0 | 0 | 10 |

当前为未完成状态，不把暂缓请求算成成功或失败。基线全部完成后才能确定哪些下游阶段确实因上游无成功结果而阻塞。

## 分批执行与用量

每次进程最多启动三个新服务请求；评分请求内部仍最多五次生成/审核尝试、总计 150 秒。相同冻结代码和参数下续跑只处理尚无结果的请求，已有失败也不重试。

| 批次 | 基线结果 | 提供方尝试 | 已记录 tokens |
|---|---|---:|---:|
| 1 | 2/3 成功 | 13 | 226,672 |
| 2 | 3/3 成功 | 12 | 224,098 |
| 3 | 2/3 成功 | 10 | 210,318 |
| 合计 | 7/9 成功 | 35 | 661,088 |

前置门槛另有 2 次调用、85,241 tokens；两者共 37 次调用、746,329 tokens。门槛用量不混入基线服务成功率。金额未推算，失败尝试可能没有完整提供方用量。

R7K2 第 2 次基线因生成超时、JSON 解析失败及满分扣分绑定错误，未取得结构合格报告。P5L2 第 1 次基线在解析失败和超时后只取得一份结构合格报告，预算不足以继续生成并审核。两次均正确拒绝发布，没有放宽结构或安全校验。解析日志中的短文本摘要包括流片段候选，不据此单独断言提供方仅返回几个字符。

## 已取得的稳定性证据

H9S2 三次独立基线均成功，总分均为 85，六维向量均为 `[100,100,99,92,100,20]`。该样本基线的总分和各维度极差均为 0，符合稳定性阈值。其他样本尚未有三份成功重复评分，不能判为稳定。此结论不覆盖优化后复评分。

## 代码与离线验证

本轮冻结了 STAR 内容类别计分、夸大风险归零前移、安全分差诊断和分批执行机制。真实运行期间未修改代码或协议。启动前相关完整后端回归 1,089 项：1,084 通过、5 跳过；此前受影响前端契约 31 项通过，前端输入未再改变。未部署、未切换模型，用户原有前端改动保持不变。

## 续跑

继续使用同一命令，保持参数和代码不变：

```powershell
python -B qa_resume_blind_final.py v4accept20260907f --v4-holdouts --audit-gate v4audit20260907g --service-batch-size 3
```

在 `backend/` 执行。当前没有 `finished.json`；`progress.json` 明确为未完成。若代码或配置变化，必须新建运行名称并保留本轮未完成证据，不能接续混算。

证据：[当前进度](progress.json)、[固定协议](protocol.json)、[冻结清单](run-manifest.json)、[前置门槛](../2026-09-06-resume-blind-v4audit20260907g/REPORT.md)、[批次 1 用量](usage-batch01.json)、[批次 2 用量](usage-batch02.json)、[批次 3 用量](usage-batch03.json)。
