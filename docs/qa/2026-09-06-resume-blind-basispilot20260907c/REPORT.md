# R7K2 单样本诊断

本次评分失败，未发布结果，也未达到服务通过率门槛。固定仅请求 R7K2 一次，不进行外层重试或替换失败样本。

- 冻结版本：`resume_evidence_basis_v2`、`resume_evidence_anchors_v4_1`。
- 耗时：92.64 秒。
- 提供方实际尝试：4 次，记录 78,855 tokens。
- 外部通知关闭；未执行优化、复评分或盲评。

第一次生成因专业表达的等级与缺陷类别矛盾而被拒绝。其后两次生成结构合格，批量审核只通过其中一份；另一份把“负责产品工作，负责相关工作，负责相关工作”这种空泛、重复的行动给到 30/35 分，STAR 行动项因无依据高分被拦截。剩余一次额度不足以再完成生成与审核，因此提前失败，没有降低发布要求。

两份有效报告的可读性审核均未指出过量标点扣分。这仅证明本次诊断未重现该问题，不证明重复评分稳定。总体、各维度的跨请求稳定性均为样本不足。

后续离线修复将专业表达程度和缺陷合为单个受限枚举，并明确 STAR 占位行动不得获得接近满分。后续代码不属于本次冻结结果，尚无新的真实请求证明修复效果。

离线修复后，完整相关后端回归运行 1,079 项：1,074 项通过、5 项按环境条件跳过；受影响前端契约 31 项通过，`git diff --check` 通过。未改动用户已有前端编辑，未部署或切换模型。结构测试可以确认拒绝与预算边界，不能证明新量表提示词的实际准确率。再次扩大真实测试前需用当前代码重新通过两轮随机量表审核门槛。

证据：[请求与诊断](score-R7K2.json)、[用量及审核理由](usage-evidence.json)、[固定协议](protocol.json)、[冻结清单](run-manifest.json)。本次失败及前轮记录全部保留。
